# --- Core Framework ---
fastapi==0.111.0          # The main web framework
uvicorn[standard]==0.29.0 # The ASGI server to run the app
pydantic-settings==2.2.1  # For managing .env files (modern approach)
python-multipart==0.0.9   # Required for file uploads (e.g., photo scanning)

# --- Database (Shared DB) ---
# Assuming PostgreSQL. If using MongoDB, swap these for 'motor' and 'odmantic'.
sqlalchemy==2.0.30        # ORM to talk to the shared database
psycopg2-binary==2.9.9    # PostgreSQL driver

# --- Authentication (Node.js Compatibility) ---
python-jose[cryptography]==3.3.0  # To decode/verify the JWTs generated by Node
passlib[bcrypt]==1.7.4            # (Optional) If you ever need to verify passwords locally

# --- AI & External Services ---
openai==1.30.1            # For Vision AI (Food scan) and NLP (Voice logging)
httpx==0.27.0             # Async HTTP client (for calling Barcode APIs)

# --- Data Processing (For your Scripts/CSVs) ---
pandas==2.2.2             # To read/manipulate your CSV files
requests==2.31.0          # To fetch URLs found in your CSVs (Synchronous)
numpy==1.26.4             # Dependency for Pandas (math operations)

# --- Dev Tools (Optional but recommended) ---
black==24.4.2             # Code formatter
pytest==8.2.0             # For testing